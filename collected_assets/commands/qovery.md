---
description: Interact with Qovery — route to the right skill or run quick operations
---

Interact with Qovery. Route to the appropriate specialized skill or handle quick operations directly.

If arguments are provided, use them to determine intent:
- `$ARGUMENTS` — action (e.g., "deploy", "status", "stop my-env", "logs backend"), service/environment name, or Qovery Console URL

If no arguments, ask the user what they want to do.

Follow the qovery skill to detect intent, auto-route to specialized skills when confident, or handle quick operations (list, status, stop, restart, logs, clone, scale) directly.
